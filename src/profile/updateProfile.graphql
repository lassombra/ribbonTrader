mutation ($currentUser: CurrentUserInput!) {
    updateCurrentUser(currentUser: $currentUser) {
        googlePicture
        name
        displayedName
        sched
        ribbons {
            id
            title
            description
            Tags {
                id
                description
            }
        }
    }
}
